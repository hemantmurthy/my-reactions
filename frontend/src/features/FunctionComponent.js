function FunctionComponent(props) {
    return (
        <div>
            <h1>Function based component</h1>
            <p>This is a function based component. The prop name is set to value {props.name}. </p>
        </div>
    )
}

export default FunctionComponent;